apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: afriswarm-postgres
  namespace: afriswarm
spec:
  serviceName: postgres
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: ankane/pgvector:v0.5.1
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: afriswarm-secrets
                  key: db-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: afriswarm-secrets
                  key: db-password
            - name: POSTGRES_DB
              value: afriswarm
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
  volumeClaimTemplates:
    - metadata:
        name: postgres-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: afriswarm
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
